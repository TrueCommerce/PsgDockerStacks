# Portainer

Portainer is a 3rd-party container used to manage smaller layouts of docker nodes and clusters.

## Deployment

```bash
git clone --depth 1 --no-checkout https://github.com/truecommerce/psgdockerstacks .
git checkout master -- portainer
cd portainer
docker-compose up -d
```
